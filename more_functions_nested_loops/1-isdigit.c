#include "main.h"

/**
* _isdigit - This function verify if c is digit
* @c: the parameter char to comparate.
* Return: 1 or 0.
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
